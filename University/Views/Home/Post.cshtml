@model University.Models.Post

@{
    ViewData["Title"] = "Post ";
}
<style>
    .li {
        border-bottom: 1px solid #e7e7e7;
        padding-bottom: 10px;
        margin-bottom: 10px;
    }
</style>
<div class="banner" style="text-align: center;background-color: red;border-radius: 160px;">
    <img style="

    width: 100%;

" src="~/img/banner.jpg" />
</div>
<div class="container" style="margin-top:100px">
    <div class="row">
        <!-- Cột bên trái để hiển thị PostCategory -->
        <div class="col-md-4">
            <h3 style="color:#106CB2"> Tin Tức & Sự Kiện</h3>
            <ul>
                @foreach (var category in ViewData["Categories"] as List<University.Models.PostCategory>)
                {
                    <li class="li"><a href="@Url.Action("PostsByCategory", "Home", new { categoryId = category.CategoryId })">@category.Name</a></li>
                }
            </ul>
        </div>

        <!-- Cột bên phải để hiển thị chi tiết của bài viết -->
        <div class="col-md-8">
            @foreach (var post in ViewData["Posts"] as List<University.Models.Post>)
            {
                <div class="row mb-3 li">
                    <div class="col-md-4">
                        <img src="/img/@post.PostImage" class="img-fluid" alt="Post Image">
                    </div>
                    <div class="col-md-8">
                        <a href="/Home/PostDetail/@post.PostId">
                            <h4>@post.Title</h4>

                            <p>Date: @post.PublishedDate</p>
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
